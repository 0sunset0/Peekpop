# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.build/
*.ipa
*.dSYM.zip
*.dSYM

# Fully generated by `xcodegen generate` from project.yml — regenerate, don't commit.
# (Also keeps xcodebuild's resolved DEVELOPMENT_TEAM out of git — it was getting baked
# into project.pbxproj by automatic signing, not from project.yml itself.)
Peekpop.xcodeproj/

# macOS
.DS_Store

# Local tooling/session state (not part of the app, changes on every run)
.omc/
